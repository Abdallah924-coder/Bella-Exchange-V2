<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Bella Exchange, plateforme d'achat et de vente de cryptomonnaies en Republique du Congo." />
  <meta name="keywords" content="crypto, Congo, Brazzaville, achat, vente, bitcoin, usdt, bella exchange" />
  <meta name="theme-color" content="#0057ff" />
  <link rel="manifest" href="/manifest.webmanifest" />
  <link rel="icon" href="/public/icon.svg" type="image/svg+xml" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body>
  <div id="universeLoader" class="universe-loader" aria-hidden="true">
    <div class="galaxy">
      <span class="ring ring-a"></span>
      <span class="ring ring-b"></span>
      <span class="ring ring-c"></span>
      <span class="core"></span>
      <span class="star star-1"></span>
      <span class="star star-2"></span>
      <span class="star star-3"></span>
      <span class="star star-4"></span>
    </div>
    <p class="loader-text">Bella Exchange</p>
  </div>
  <header class="site-header">
    <a href="/" class="brand">Bella Exchange</a>
    <nav>
      <a href="/">Accueil</a>
      <a href="/buy">Achat</a>
      <a href="/sell">Vente</a>
      <a href="/contact">Contact</a>
      <% if (currentUser && currentUser.role === 'admin') { %>
        <a href="/admin">Admin</a>
      <% } %>
      <% if (currentUser) { %>
        <a href="/profile">Profil</a>
      <% } %>
    </nav>
    <div class="header-actions">
      <button id="themeToggle" class="btn small">Theme</button>
      <% if (!currentUser) { %>
        <a class="btn small" href="/login">Connexion</a>
      <% } else { %>
        <form method="POST" action="/auth/logout">
          <button class="btn small" type="submit">Deconnexion</button>
        </form>
      <% } %>
    </div>
  </header>
  <main>
